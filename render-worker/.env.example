# Paste the values Lovable generated for you (Project → Secrets)
RENDER_WORKER_TOKEN=replace-me
RENDER_CALLBACK_SECRET=replace-me

# Optional
PORT=8787

# Traefik / TLS
RENDER_HOST=render.amazsocials.com
TRAEFIK_NETWORK=root_default
TRAEFIK_CERT_RESOLVER=mytlschallenge
